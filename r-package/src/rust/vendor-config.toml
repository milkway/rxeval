# Written by `cargo vendor`, and kept: without it the extracted sources
# are ignored and the build reaches for the network, which CRAN machines
# do not have.
[source.crates-io]
replace-with = "vendored-sources"

[source.vendored-sources]
directory = "vendor"
