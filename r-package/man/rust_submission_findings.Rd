% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rust_submission_findings}
\alias{rust_submission_findings}
\title{What a form's own rules say about a submission.}
\usage{
rust_submission_findings(form, submission, today, now)
}
\arguments{
\item{form}{Path to an XForm, or the XML itself.}

\item{submission}{Path to a submission, or its XML.}

\item{today, now}{What \code{today()} and \code{now()} should answer. Defaults to
the submission's own metadata, so a date rule is judged against the
day the work was done rather than the day of the check.}
}
\value{
A data frame: \code{path}, \code{kind}, \code{says}. Zero rows means the
submission satisfies every rule the engine could evaluate — and a
rule it could not evaluate appears as kind \code{not-evaluated}, never as
a pass.
}
\description{
What a form's own rules say about a submission.
}
