% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rxeval.R
\name{submission_findings}
\alias{submission_findings}
\title{What a form's own rules say about a submission}
\usage{
submission_findings(form, submission, today = NULL, now = NULL)
}
\arguments{
\item{form}{Path to an XForm, or the XML itself.}

\item{submission}{Path to a submission, or its XML.}

\item{today, now}{What \code{today()} and \code{now()} should answer. Left \code{NULL},
they come from the submission's own metadata, so a date rule is judged
against the day the work was done rather than the day of the check —
otherwise a submission that was valid on collection starts failing
later, and the report changes while the data does not.}
}
\value{
A data frame of \code{path}, \code{kind} (\code{constraint}, \code{required},
\code{calculation}, \code{not-evaluated}) and \code{says}. Zero rows means every rule
held.
}
\description{
Evaluates the questionnaire's \code{relevant}, \code{constraint}, \code{required} and
\code{calculate} against collected data, and reports what does not hold.
}
\details{
A rule the engine could not evaluate comes back as \code{not-evaluated}, never
as a pass: a rule that did not run has not been satisfied, and reporting
it as clean would be the one failure worth avoiding.
}
\examples{
\dontrun{
findings <- submission_findings("survey.xml", "submission.xml")
table(findings$kind)
}
}
